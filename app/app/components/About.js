const React = require('react');

const About = () => {
    return (
        <div className="container text-center">
            <p>This is a Celebrity Hologram Store application which includes a frontend and backend API, along with a database.</p>
        </div>
    );
};


export default About;